# Workflow Efficiency P1–P10 Verification and Rollback Contract v1

**Date:** 2026-08-21

**Regime:** B

**Status:** `[PROPOSED]`; inoperative pending CRITIC and Rebecca

| Proposal | Exact affected text/artifact | Owner after approval | STOP/failure condition | Required verification | Incremental rollout | Rollback |
|---|---|---|---|---|---|---|
| P1 canonical routing | Coordinator `Routing protocol`; `workflow_routing_table_v1.json`; conforming universal init insertion | TASK BUILDER mechanical; Coordinator owns live routing | Missing/full-SHA-conflicting override; omitted mandatory role/gate | Validate four defaults, BLOCK return, additive override, prohibited removal | Stage 1 with P7 | Revert Stage-1 commit; legacy strict serial routes resume |
| P2 handoff manifest | Seven init handoff sections; `common_handoff_manifest_schema_v1.json` | TASK BUILDER mechanical; each sender authors its instances | Wrong sender extension; incomplete/duplicate/path-invalid artifact inventory; routing-head/result mismatch; schema/unknown field; non-full SHA; task/session ID; non-repo authority | Valid/invalid instance for every sender; wrong-extension, omitted-artifact, normalized-duplicate, absolute/backslash/parent-path, distinct-valid and invalid-head/result fixtures | Stage 2 before tooling | Revert schema/init references; retain readable historical Markdown |
| P3 executability trace | ARCHITECT source trace; separate CRITIC/TASK BUILDER dispositions; both schemas | ARCHITECT authors trace only; each receiver authors only its digest-bound disposition | Any `STOP_UNRESOLVED`; row/order/digest/verification-ID mismatch; implementer choice | Complete/unresolved traces; missing/extra/reordered/duplicate rows; wrong source/row digest; cross-role edit prohibition | Stage 2 with P2 | Revert prospective trace/disposition requirement; preserve three existing defenses |
| P4 freshness/precedence | Coordinator startup/checkpoint text; state metadata template; source map | Coordinator/INTEGRATOR/RECORDER within owned files | Current-source factual divergence; invalid timestamp/SHA/status | Current, stale, superseded, and contradictory document fixtures | Stage 3 | Revert metadata only; preserve all historical files/content |
| P5 preflight tooling | Policy §3/§3.1; public-safety sections; v2 report schema; future tool/tests | TASK BUILDER tool; every pushing role manual review | Any nonzero exit; scanner absent/error; incomplete ancestor/merge range; prohibited finding | Exit codes 0/2/3/4/5; add→modify, add→delete, rename→modify, type-change→delete; multi-domain dedup/provenance; combined-domain identity; canonical bytes; self-literal retained and real-path blocker | Stage 4 after schemas | Disable/remove helper; manual full-range scans remain mandatory |
| P6 immutable checkout | Universal init insertion; Coordinator branch guidance; future helper | TASK BUILDER helper; each role verifies | Marker/ref/SHA mismatch; branch/path collision; dirty/shared/nested worktree; unsafe cleanup | Exact ref success; zero/multiple/mismatch ref; missing marker/object; branch/path collision; dirty/nested checkout; receipt/digest/HEAD/cleanup-boundary tests | Stage 4 with P5 | Remove helper; manual isolated exact-ref/exact-SHA checkout remains |
| P7 safe batching | Coordinator routing/subagent sections; INTEGRATOR/RECORDER text; routing table | Coordinator routes; owners retain files | Dependency, shared output, self-review, scoring/seed work, missing commit order | Independent concurrent preparation; dependency rejection; batched-state serial attestation | Stage 1 with P1 | Revert to strict serial one-event cycles |

Stage 1 P1/P7 verification is executable only through `specs/data/workflow_stage1_validator_contract_v1.json` over the ordered corpus `specs/data/workflow_stage1_routing_fixtures_v1.json`, each case conforming to `specs/data/workflow_stage1_route_case_schema_v1.json`. All prescribed cases must match byte-for-byte expected canonical output; any missing, extra, reordered, or mismatched case is STOP `[PROPOSED]`.
| P8 policy metadata | Policy header/effective text and changelog only | TASK BUILDER exact mechanical edit after approval | Text asserts public flip; substantive policy changes; wrong Entry 57 citation | Diff limited to metadata/changelog; CRITIC verifies Entry 57 | Stage 5 | Revert metadata commit; approval evidence remains |
| P9 session-ID ban | Universal init insertion; policy §3; handoff/checkpoint templates; scan regex | TASK BUILDER mechanical; Coordinator keeps ephemeral map | New public task/thread/session ID or URL; map persisted as authority | Positive/negative regex fixtures; historical files unchanged | Stage 5 with P8 | Revert prospective template rule only by later Rebecca approval |
| P10 JUDGE custody | JUDGE step/handoff; RECORDER custody; Coordinator scoring route; manifest extension | JUDGE authors bytes; RECORDER publishes/custodies | Byte/hash mismatch; scan block; publication unavailable; RECORDER edit | Exact-byte success, mismatch, scan block, publication failure, Rebecca activation boundary | Stage 5 after P1 route | Revert prospective mechanics; preserve every published ruling/provenance entry |

No rollback may delete evidence, relabel a failure, rerun scoring, expose seeds, weaken P1–P6, or merge without Rebecca `[PROPOSED]`.

Rollback authority and transitions are exclusively those in `specs/data/workflow_stage_rollback_v1.json`. The dependency cascade is `S1(P1/P7) → S2(P2/P3) → S3(P4) → S4(P5/P6) → S5(P8/P9/P10)`; target plus descendants roll back, ancestors remain. `IMPLEMENTING` failure/CRITIC BLOCK and `IMPLEMENTATION_VERIFIED` pre-release failure/non-release both transition to `SUSPENDED`; negative fixtures prove no direct retry or release transition. ARCHITECT proposes, fresh-context CRITIC clears, Rebecca authorizes, file owners execute, fresh-context CRITIC verifies, and Rebecca alone merges/releases `[PROPOSED]`.

Every schema has an implementation validator for cross-field invariants that JSON Schema cannot express. Required negative fixtures include: incomplete artifact inventory; sender/extension mismatch; trace row/order/digest/overall inconsistency; state owner-role/document-role mismatch; non-sequential path-event indices; domain/order mismatch; invalid endpoint projection; finding dedup/order/domain mismatch; routing-head/result ancestry or post-result non-handoff change; and every unlisted rollback transition `[PROPOSED]`. Schema validation alone never passes these fixtures.
