# REBECCA RULING — L8 GPU Adoption Pre-Registration SIGNED + Full-Screen GPU Run Release + TASK BUILDER Release

**Date:** 2026-08-21 · **Regime:** B (post-Entry 81; `docs/ARCHITECTURAL_CONSTITUTION_v2.md` §5 binding) `[P4]`
**Principal:** Rebecca R. McClintic — APPROVED 2026-08-21 00:54–00:56 EDT (recorded at her direction by the WORKFLOW COORDINATOR; no agent speaks for Rebecca)
**Status:** OPERATIVE

---

## Ruling

Following the fresh-context CRITIC's CLEAR on the L8 GPU diagnostic-backend adoption specification v1.4 (`reviews/critic_l8_gpu_adoption_v1.4_rereview.md` on `critic/l8-gpu-adoption-v1.4-rereview`; LAW_FIDELITY: PASS, SUBSTANTIVE: CLEAR), Rebecca rules:

1. **Pre-registration SIGNED (00:54 EDT).** The `[PROPOSED]` criteria of the L8 GPU adoption specification v1.4 are approved. The contract is operative as a pre-registration.
2. **Full-screen GPU run RELEASED (00:56 EDT).** Rebecca's explicit release of the 19.2M full-screen GPU diagnostic, as required by the GPU rebuild ruling (`docs/rulings/REBECCA_L8_FULLSCREEN_GPU_REBUILD_APPROVAL.md`, PR #105, which does not auto-release on clearance).
3. **TASK BUILDER RELEASED (00:56 EDT).** Rebecca's explicit TASK BUILDER authorization, the distinct gate recorded in the geometry-table freeze ruling (`docs/rulings/REBECCA_L8_GEOMETRY_TABLE_FREEZE.md`).

## Scope and boundaries

- Execution scope: the CPU↔GPU equivalence sentinel + the 19.2M full-screen GPU diagnostic per the operative v1.4 pre-registration, on Rebecca's workstation. **Diagnostic-only (O-15)**; synthetic candidate-blind seeds only.
- The CRITIC's non-blocking clarity findings NB-J–NB-M are noted; per the CRITIC they do not gate this decision and may be closed in a later editorial pass or carried into the full-screen contract at Rebecca's discretion.
- This ruling does NOT authorize: scoring; protected/hold-out seed access (seeds 201–203, 301–303 never accessed or rerun — O-14); any G2–G4 freeze; spec-text changes (P5); native GPU calibration or torch-native RNG adoption; CPU replacement; automatic retry/fallback; renaming of negatives; L15/L16/L17 work before M5; merge to main beyond this record.

## Route

TASK BUILDER (executes per `handoffs/WORKFLOW_COORDINATOR_L8_FULLSCREEN_GPU_TASKBUILDER_RELEASE_HANDOFF.md`) → INTEGRATOR → CRITIC (implementation review) → RECORDER → Rebecca.

## Standing constraints (unchanged, binding)

O-14, O-15, D1–D5, L9, L18, candidate-blindness (Ruling 9), no-relabeling, ≥2 unseen scoring seeds, no renaming negatives, §5 P1–P6, Rebecca sole gate/merge authority.

## Pre-push safety scan attestation

Ruling record. Contains only public repo SHAs, branch names, file paths, ruling references, dates, and routing. No credentials/secrets/PII/machine identifiers/private paths/protected seeds. Scan result: **clean**.
