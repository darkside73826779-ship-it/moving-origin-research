# CRITIC M4 Post-Tokenizer CC1–CC6 Rereview

Date: 2026-08-21 EDT  
Regime: B  
Role: authoritative persistent CRITIC  
Status: **COMBINED BLOCK**

## Immutable intake and identity validation

- Substantive head: `architect/m4-post-tokenizer-crash-cart-cc1-cc6-remediation @ 9ea956a355aa4524c527ccf09ca1b2882e89c40b`
- Review result: `4c195be0966845ccac215da7d197397b945f173d`
- Base: `390b06dadd2a364160d851c19cf85927465213d0`
- Canonical manifest: `architect/m4-post-tokenizer-crash-cart-cc1-cc6-remediation-manifest @ a6e6b9daf5d670a7e13c5a112c848d51288c05bf`
- Prior CRITIC BLOCK: `critic/m4-post-tokenizer-integration-crash-cart-design-review @ 1cd82cfda155381fae1a1d9e176a1fe67c265dae`
- Checkout: repository `tools/workflow_checkout.py`; receipt SHA-256 `71067127f91ca7bae90a042b6d8c4db2d7724e3a80be898c00bcaf170774a36c`

The canonical manifest validates with `tools/workflow_contract_validator.py`. All eleven declared raw Git-blob identities reproduce exactly. The four JSON artifacts are strict one-line UTF-8 JSON with one terminal LF; all four sidecars bind exact bytes. All eighteen embedded RFC-8785 object digests and all four deterministic private-view byte lengths/digests independently reproduce. The base-to-head delta is exactly the eleven declared design-only paths.

## Banked closure evidence

- CC1 now defines the role/arm table, backend signatures, ownership boundaries, receipt core, and primary factory/isolation/channel codes.
- CC2 introduces terminal-request completion, completion-gated reset, in-flight/reentrancy guards, state×operation rules, and ordinal/episode codes.
- CC3 provides a deterministic public generator, exact canonical private bytes, 1024/4096/8192 plus stop digests, canonical object hashes, two-episode four-step traces, and independently named close adapters.
- CC4 directly binds the private-view encoding and key digest/mutation/association/stop/atomicity codes.
- CC5 correctly describes the substantive L7/L8/L10/L14/L18 meanings and honestly projects current laws as HELD without selecting Q2/EF3/native-L8/scientific results.
- CC6 freezes a 36-path tokenizer overlay, exact 37-rule tokenizer attributes block, source-SHA designation, fixed seam names, and a topological rebinding concept.
- Public-safety inspection found no credentials, private paths, custody values, real token arrays/IDs, model/tokenizer bytes, protected seeds, scores, or scientific results.

## Batched residual findings

### R-CC1 — Backend negative matrix does not cover the declared receipt contract

The protocol declares `BACKEND_RECEIPT_INVALID`, `BACKEND_SESSION_MISMATCH`, and `BACKEND_STATE_MISMATCH`, plus registered backend `status=FAIL` with no adapter-state commit. None has an exact executable-fixture row. The claim that the fixture binds one row per code is therefore false.

Add exact request/prior-token/receipt mutations, expected backend-call delta, first-failure code, and before/after durable-state identity for every declared backend and factory failure.

### R-CC2 — Lifecycle matrix and synthetic states remain incomplete and reentrancy is nondeterministic

The fixture selects a few lifecycle negatives but does not realize every invalid state×operation cell, repeated describe/initialize/close, every operation after CLOSED, `EPISODE_ID_MISMATCH`, lifecycle-invalid snapshots, or all reset/request/snapshot failures. It lacks canonical initialize/reset/snapshot/close requests and receipts, backend state tokens, CLOSED states, and most negative before/after digests. Close sequences remain state-name labels rather than independently hashed traces.

The transient lock stores operation id but no caller/thread/session identity, so it cannot mechanically distinguish same-caller reentrancy from another caller’s in-flight conflict. Bind the caller identity domain and complete exact per-cell fixtures/traces with zero/one backend-call expectations and byte-identical failure state.

### R-CC3 — Universal zero-backend-call assertion contradicts backend-derived failures and incomplete pair identity

Fixture `negative_universal.backend_call_delta=0` applies to the entire matrix, including `BACKEND_EXCEPTION` and `RESPONSE_CORRELATION_FAILURE`. Those conditions can only be observed after a backend invocation or receipt, so zero calls is impossible. Split pre-call suppression cases from post-call/no-commit cases and bind exact counts.

Candidate/peer fixture manifests also omit most promised equality fields—tokenizer, architecture, parameters, quantization, decoding, calibration, evaluation, and binning—so the fixture cannot prove the full same-checkpoint/training equality set. Bind every equality/difference field and its mutation evidence.

### R-CC4 — Fanout atomicity rules and exact negative matrix conflict

The prose says every upstream failure suppresses all real-backend calls, then describes “candidate succeeds but peer pre-call hashing fails.” Candidate success entails a backend call. A true pre-call peer hash failure must occur before either backend invocation; otherwise the rule must explicitly allow the candidate call while forbidding public commit.

`FANOUT_ATOMICITY_FAILURE` is absent from the fixture matrix, and the six sanitized reconciliation negatives from the base design are referenced but not carried into the new exact executable matrix. Bind a two-phase pre-call validation rule, or exact asymmetric call/no-commit semantics, plus every fanout/reconciliation row and state digest.

### R-CC5 — Versioned-law source and L8 HELD semantics are inconsistent

The law artifact cites unversioned `docs/ARCHITECTURAL_CONSTITUTION.md` and old line numbers. The binding role contract requires the versioned law source `docs/ARCHITECTURAL_CONSTITUTION_v2.md`; the relevant lines there are L7 26, L8 28, L10 32, L14 42, and L18 54. Although the quoted meanings agree, active provenance must bind the governing versioned source.

`L8_PREREQUISITE_HELD` is also listed as a deterministic/allowed FAIL code while HELD rows correctly require `failure_code=null` and use `held_reason=L8_PREREQUISITE_UNCLEARED`. Remove the held condition from FAIL codes and bind it only as a HELD projection. Preserve the honest no-claim posture and all scientific holds.

### R-CC6 — Frozen combined-tree construction cannot carry the tokenizer result and leaves seam bytes unbound

The exact 36-path overlay includes only `artifacts/m4_tokenizer_materialization/.gitkeep`; it omits both required post-tokenizer source artifacts:

- `artifacts/m4_tokenizer_materialization/tokenizer_materialization.json`
- `artifacts/m4_tokenizer_materialization/tokenizer_materialization.json.sha256`

The design says integration receives the sanitized result plus its raw identity, forbids extra overlay paths, and takes non-overlay paths unchanged from `MAIN_SHA`. A result pair existing on `TOKENIZER_SHA` therefore cannot enter the combined tree. The currently consumed pair is FAIL/empty and is not a usable positive integration input, but the structural omission would also exclude any future cleared pair.

The exact attributes append block also has no `text eol=lf` rules for the four fixed seam paths (`src/__init__.py`, implementation module, selected test, and wrapper), and no precondition requires `MAIN_SHA` to supply them. Their later raw mounted identities are therefore checkout-platform dependent despite the package’s exact-byte requirement.

Revise the frozen overlay/result-state rule, including an explicit stop when no cleared PASS pair exists; add exact seam attribute rules and their rebinding cascade; preserve deterministic source designation and no-merge construction.

## Law fidelity, safety, and holds

The package remains design-only and correctly separates real backend selection, Q2, EF3, native L8, qualification, scoring, seeds, and science. The findings require mechanical correction only and authorize none of those held decisions.

Review preflight findings F000001 and F000002 are one occurrence duplicated across commit-parent and combined-range domains, wholly inside the required public base commit identity on line 12. They are classified as non-secret reproducibility metadata and are not suppressed. Other scanner findings wholly inside required public commit, artifact, state, request, receipt, sidecar, or synthetic digests require the same explicit classification. Gitleaks findings: zero. No implementation or governed execution occurred.

## Disposition

**COMBINED BLOCK.** Return through WORKFLOW COORDINATOR to ARCHITECT for one batched R-CC1–R-CC6 remediation. Preserve all banked deterministic digest, role/arm, lifecycle, fanout, law-meaning, source-separation, and public-safety evidence. This review is independent of TASK BUILDER token-ID remediation and changes no execution state or hold.
