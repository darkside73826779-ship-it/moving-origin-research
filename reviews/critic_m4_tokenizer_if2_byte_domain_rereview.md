# CRITIC Rereview — M4 Tokenizer IF2 Runtime Byte Domain

**Date:** 2026-08-21  
**Regime:** B  
**Gate served:** Narrow independent IF2 worktree-byte-domain, LF checkout construction, identity, isolated-discovery regression, law-fidelity, and public-safety rereview before any re-release of the still-unconsumed tokenizer materialization operation.

## Inputs and lineage reviewed

- Authority: `coordinator/m4-tokenizer-exact-image-corrective-rerelease@d7f9e984052af3088662be852bbdb0cbb5069f45`.
- Prior review: `critic/m4-tokenizer-test-package-marker-if1-rereview@863e886f19dfe4aaa898ca71b2ed9bacb4c4f94a`.
- Prior input: `architect/m4-tokenizer-materialization-spec@6d040083a7fe7dddfda351e863dc8940ca25689f`.
- Remediated input: `architect/m4-tokenizer-materialization-spec@619ff927131222071bdc168f38f40a9760fd5476`.
- Handoff: `handoffs/ARCHITECT_TO_COORDINATOR_M4_TOKENIZER_IF2_BYTE_DOMAIN_CHECKOUT_REMEDIATION_2026-08-21.md`.
- Reviewed the complete `6d040083a7fe7dddfda351e863dc8940ca25689f..619ff927131222071bdc168f38f40a9760fd5476` delta. No OCI, model, tokenizer, custody, or materialization operation was accessed or executed.

## First checklist item — Versioned-Law Compliance Protocol

The delta changes no constitutional quotation and introduces no reconstructed law, scientific threshold, bar, scoring criterion, provenance citation, or waiver. The new `.gitattributes` artifact states date `2026-08-21` and Regime B, and every new checkout/identity criterion remains `[PROPOSED]`. Prior P1–P6 evidence remains intact.

**LAW_FIDELITY: CLEAR.**

## Independent IF2 verification

- The runtime marker identity domain is now explicitly `RAW_MOUNTED_WORKTREE_FILE`; Git-object substitution and post-checkout normalization/copy are forbidden.
- `.gitattributes` commits the exact rule `tests/__init__.py text eol=lf`. Its normalized LF Git blob is exactly 61 bytes with SHA-256 `8075b59d03e71487b74ee4ecce172dd66c0b4c1b291304424fe538e0e66dffa5`.
- The preserved marker is exactly 69 LF bytes with SHA-256 `6f56eb2128751f0c5c1ab27ff461b38565aa00a3dcd29c04ab8bd56c34f4a961`.
- The revised test-contract LF bytes hash to `b83faf6fdcf808b89db96291acd730e626824624265630fd7f107e883c365252`. All three identities match the specification, test contract, sidecar where applicable, and handoff.
- The prescribed construction fixes a fresh clean released-full-SHA checkout, exact attribute verification, binary worktree-file verification, and read-only mounting of that same checkout. Reused/dirty checkouts, attribute overrides, generated/copied/normalized markers, byte mutation, and Git-object substitution fail closed before custody lookup.
- I independently created a fresh detached worktree at exact SHA `619ff927131222071bdc168f38f40a9760fd5476`, without modifying its files. It was clean; `git check-attr text eol -- tests/__init__.py` returned `text: set` and `eol: lf`; the checked-out marker was exactly 69 bytes and matched the bound SHA-256. The temporary verification worktree was then removed.
- The `.gitattributes` byte identity is expressly a normative LF Git-blob identity; runtime marker validation separately targets the raw mounted worktree file. The two domains are no longer conflated.
- The isolated discovery command and materializer command are exactly unchanged from `6d04008`. Banked IF1 package importability, RF1 classifier, LF1/P4 metadata, pinned image/runtime, custody/schema/fixture identities, and the unconsumed operation remain unchanged.
- `git diff --check` passes.

**IF2 is closed.** The exact cross-checkout marker construction is executable without implementer invention.

## Findings

**Blocking findings:** None.  
**Non-blocking findings:** None within the authorized scope.

## Preserved evidence

- IF1 marker identity/importability and all prior command/runtime closures remain valid.
- Both commands, pinned OCI identities, RF1/LF1 evidence, custody/schema/fixture contracts, and the single unconsumed operation remain preserved.
- No prior instrument failure is renamed or rerun.

## Verdict and routing

**SUBSTANTIVE: CLEAR.**  
**COMBINED DISPOSITION: CLEAR.** IF2 is closed and all banked evidence is unchanged.

**Exact next authorized role:** WORKFLOW COORDINATOR receives this rereview and stops for Rebecca's exact re-release decision. This CLEAR does not itself authorize TASK BUILDER execution.

**Explicitly prohibited actions:** Custody/model/tokenizer access; OCI or materialization execution/modification; inference/serving; Q2/EF3; qualification; diagnostics/scoring; protected seeds; science; STATE/provenance mutation; publication; rerun; merge; and inferred gate decision.

## Public-repository safety and prohibited-run confirmation

Public-safety scan: gitleaks 8.30.1 over the complete remediation commit range and this review artifact, required regex checks, and manual content inspection; 0 findings, cleared. No custody/model/tokenizer access, OCI or materialization execution, scoring, rerun, protected-seed exposure, state/provenance mutation, publication, or unauthorized merge occurred.
