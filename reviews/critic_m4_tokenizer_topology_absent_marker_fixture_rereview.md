# CRITIC M4 Tokenizer Topology Absent-Marker Fixture Rereview

Date: 2026-08-21 EDT  
Regime: B  
Role: authoritative persistent CRITIC  
Terminal state: **COMBINED CLEAR**

## Immutable intake and validation

- Substantive/routing head: `architect/m4-tokenizer-topology-absent-marker-fixture-reconciliation @ 14a94cbc4c2d20a1aeaa917b71e6dc63c0a174e1`
- Review result: `a0611a12e86ce2377e15388c3f0d40cc99e979c9`
- Base corrective release: `1e2e910adff95ce47bcbef1c28eff8213b71acae`
- Canonical manifest head: `57a6be6e56cf38a5ded050cbf1603be0d50f5672`
- Authoritative prior BLOCK: `taskbuilder/m4-tokenizer-private-binding-corrective-execution @ 960a988f08e029e5e89913a6a744145c52481baf`
- Helper-managed checkout receipt SHA-256: `bab6a50d34b08d75797fc010b5fff7aa89e4d2652cbe5573a768511a04bf57bb`

The canonical handoff manifest validates with the repository workflow contract validator. All twelve declared raw Git-blob identities reproduce exactly. The substantive delta is confined to the runner, its exact contract/specification/test/package rebinding, and routing artifacts. The runner remains Git mode `100755`; all three changed JSON sidecars bind exact raw bytes and the complete nine-entry executable-package inventory reproduces.

## Fixture correction

The prior failure is closed exactly. `run_absent_marker()` creates `root` with `mktemp -d`, creates `snapshot` strictly beneath that root, copies the released checkout into the snapshot while excluding `.git`, and executes:

`rm -rf -- "$snapshot/artifacts/m4_tokenizer_materialization"`

The removed target is therefore only the copied snapshot's exact relative output subtree. The command cannot name the released checkout. The obsolete marker-only `rm -f` plus `rmdir` sequence is absent, so the copied historical result pair can no longer keep the negative fixture directory nonempty. The subsequent nested mount targets the now-absent path, expects Docker engine exit `125`, requires an empty stage, maps fail-closed to `RUNTIME_IDENTITY_MISMATCH_NO_CUSTODY_NO_CONSUMPTION_STOP_NO_RETRY`, and remains inside the root-level cleanup trap.

The OCI contract, test contract, normative specification, selected static regression, and runner all bind the same `DISPOSABLE_MKTEMP_SNAPSHOT_ONLY` scope and exact relative subtree. They explicitly assert that neither the immutable release nor the historical pair is mutated.

## Preservation and regression

- The historical consumed result remains byte-identical: result 1,740 bytes / SHA-256 `10a14c1a4f7b257aa195636ab86b7a85a68fc084af13bead7b5d4b3ecfa55728`; sidecar 97 bytes / SHA-256 `da7a294daf21aa308cb0b7ac83dafba85323c2e0667a33a0844829a18b86756a`.
- `.gitattributes`, the production materializer, and the pre-import wrapper are byte-identical to the corrective release.
- The positive mount case, reversed-order case, nonempty-stage case, zero-access declarations, stage-empty assertions, failure mapping, and root cleanup are unchanged outside identity rebinding.
- `bash -n` on the exact runner passed.
- The focused no-custody contract/static production regression `test_oci_launch_contract_is_fail_closed` passed independently.
- Prior governed evidence records exact pinned wrapper 37/37 PASS, positive topology PASS/empty stage, and the old fixture stopping before Docker. The prior disposable one-line feasibility run records all four topology cases and cleanup PASS. This rereview performed no OCI or materializer execution.

The executable-package object's `single_materialization_operation=CONSUMED` continues to describe the permanently preserved historical package/result. The exact Rebecca corrective-release artifact separately and unambiguously declares the corrected new operation **UNCONSUMED**; the runner and launch/test contracts preserve zero consumption before any later separately authorized materializer start. No operation state was changed by this design correction or review.

## Law fidelity, public safety, and disposition

The change is a custody-free topology-fixture correction only. It preserves fail-closed ordering, immutable evidence, exact identities, no retry, and the public/private boundary. No custody/model/tokenizer input, private binding, materializer start, scoring, seeds, science, durable-state mutation, merge, publication, or gate decision occurred.

Public-safety review found no credentials, contact data, private paths or binding values, custody data, model/tokenizer bytes, token arrays, seeds, scores, or scientific output. Preflight findings F000001–F000012 are fixed-regex duplicate-domain matches wholly inside required immutable public commit/artifact SHA-256 values; all twelve are non-contact reproducibility metadata, manually classified here, and none is silently suppressed. Gitleaks findings are zero.

**COMBINED CLEAR.** The absent-marker disposable-snapshot correction and affected identities/contracts/regressions are internally consistent. Return to WORKFLOW COORDINATOR for any separate exact new-operation re-release decision; this CLEAR itself authorizes no execution.
