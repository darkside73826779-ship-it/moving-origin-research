# ARCHITECT to WORKFLOW COORDINATOR — M4 Tokenizer Isolated Test-Command Remediation

**Date:** 2026-08-21

**Regime:** B

**Disposition:** READY FOR PERSISTENT-CRITIC REREVIEW; EXECUTION HELD

Authority is `coordinator/m4-tokenizer-exact-image-corrective-rerelease@d7f9e984052af3088662be852bbdb0cbb5069f45`. Input is `architect/m4-tokenizer-materialization-spec@d94c059645dba018bbf2d9adf1b31c388dfcd2a3`. TASK BUILDER returned `INSTRUMENT FAILURE / CONTRACT-RUNTIME INCOMPATIBILITY` after the prior isolated dotted-module command could not import the mounted repository; no repository artifact or materialization operation resulted.

The test command is now exactly `python3 -I -m unittest discover -s tests -t . -p test_m4_tokenizer_materialization.py`, run from the mounted repository root. The contract fixes the standard-library discovery mechanism, start directory, top-level directory, filename pattern, singleton selected test file, successful project import/full-suite execution/exit-0 expectation, and no custody lookup or materialization output. It forbids `PYTHONPATH`, installation, image/path mutation, alternate runtime, fallback, and application-written `sys.path` mutation.

The revised test-contract LF Git-blob SHA-256 is `799435d725468cf1ff848e351e7bbab0252e851710a8ad0601464a94dc1686cd`.

The pinned image identities, `python3` interpreter, exact materializer command, custody/schema/fixture contracts, and all banked evidence are unchanged. The single operation remains unconsumed. No custody/model/tokenizer access; OCI execution/modification; inference/serving; Q2/EF3; qualification; diagnostics/scoring; seeds; science; STATE/provenance; publication; merge; or additional operation occurred. Final scan, commit, push, and equality are reported in the formal return.
