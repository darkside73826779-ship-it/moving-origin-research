# REBECCA AUTHORITY — CRITIC CLEAR DURABILITY, LEDGER, AND MAIN CHECKPOINT

Date: 2026-08-22 EDT  
Regime: B  
Authority: Rebecca, conveyed and durably routed by WORKFLOW COORDINATOR

## Recovery principle

Rebecca requires an authoritative persistent-CRITIC `COMBINED CLEAR` to produce a durable recovery checkpoint. The cleared package, its review evidence, and the current Coordinator disposition must not remain recoverable only by reconstructing scattered topic branches.

For each accepted authoritative `COMBINED CLEAR`, WORKFLOW COORDINATOR shall ensure that:

1. the exact cleared package and review refs are remotely equal and remain published;
2. `state/COORDINATOR_LEDGER.md` records the exact package, review, disposition, holds, current owner, and next event;
3. the ledger update is committed and pushed;
4. the exact cleared package and review evidence are integrated into remote `main` through the authorized INTEGRATOR workflow;
5. remote `main` equality and repository integrity are verified after integration; and
6. the resulting `main` commit is reported as the canonical restart point from which a replacement team can recover the project without reconstructing hidden chat state.

## Meaning of checkpoint merge

A durability merge records reviewed repository truth. It does not broaden the cleared package's operational authority.

Design-only, implementation-only, `run_authorized=false`, held, non-scoring, or inoperative artifacts retain those exact states after merge. A checkpoint merge does not authorize execution, private access, scoring, science, qualification, publication of scientific results, or a final gate decision.

Any active BLOCK, unresolved dependency, or missing future identity must remain plainly recorded in the ledger and remain fail closed after the merge.

## Immediate M4 checkpoint

Rebecca authorizes one exact durability checkpoint for the cleared M4 public-model non-scoring observation backend.

Exact inputs:

- Current remote `main` base: `69ed2929881a2dd1f0721c934225235b2f7b0f75`.
- Cleared package: `taskbuilder/m4-public-model-observation-backend-bf1-zero-count-remediation` at `11eebe60b18e325fe64b9a1ea4a5cdbf378459c7`.
- Implementation result: `46252865c5a13a997396ac7520eb3c06d8f541e1`.
- Routing result: `9076386c6a51159bd977035ceaf431ab17c90e5e`.
- Authoritative persistent-CRITIC review: `critic/m4-public-model-observation-backend-bf1-zero-count-rereview` at `98b8f466db2ad8499ea5abab9111a68a9bd1e861`.
- Review artifact: `reviews/critic_m4_public_model_observation_backend_bf1_zero_count_rereview.md`.

INTEGRATOR shall construct the smallest exact combined tree that preserves current `main`, adds the cleared package and review evidence, and updates the Coordinator ledger with the restart state. It shall not include the currently blocked final crash-cart design as a cleared artifact.

The ledger must also record that:

- the observation backend is banked `COMBINED CLEAR` but remains `run_authorized=false`;
- the final pre-scoring crash-cart design review is `COMBINED BLOCK` at `9a028cb0799807910900f29ef8a1ff8cb4af0b4d` pending the exact Architect BF1–BF4 correction;
- the durable crash-cart user directive is on `coordinator/m4-final-crash-cart-rebecca-authority` at `8b4a383c450e8b29f2633f7e25107abcb62dd929`; and
- no protected scoring release exists.

## Integration safety

INTEGRATOR must use exact remote identities and a clean isolated checkout, preserve unrelated current-main bytes, resolve no substantive conflict by invention, validate all affected manifests and sidecars, run proportionate custody-free regression checks, scan the complete introduced range, and verify remote equality after push.

If current `main` has advanced, the exact new base must be reported and the combined tree must preserve the intervening commits. If a substantive conflict, invalid manifest, incomplete inventory, or prohibited content prevents an exact merge, INTEGRATOR must stop and return a durable BLOCK rather than silently omitting evidence or rewriting history.

## Holds

This authority does not permit:

- model, tokenizer, OCI, gofast, or observation execution;
- private custody or protected-input access;
- scoring, qualification, Q2, EF3, L8 science, or protected-seed work;
- changes to scientific laws, thresholds, controls, or seed rules;
- activation of the observation backend;
- inclusion of uncleared crash-cart design bytes as a cleared package;
- scientific-result publication or a final readiness/gate decision.

## Expected next event

WORKFLOW COORDINATOR routes this exact authority, the cleared package, and the authoritative CRITIC review to INTEGRATOR. INTEGRATOR publishes one exact combined checkpoint, updates and pushes the Coordinator ledger, advances remote `main` only after all checks pass, and returns the new canonical restart SHA to WORKFLOW COORDINATOR.
