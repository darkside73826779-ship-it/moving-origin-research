# REBECCA DIRECTIVE — L8 BACKEND CODENAMES

**Date:** 2026-08-21

**Regime:** B

Rebecca R. McClintic assigns the following exact, case-sensitive codenames:

- `gofast` — the parallel-CPU L8 simulator/evaluator.
- `GO!` — the serial-CPU L8 simulator/evaluator.
- `go faster` — the native-CUDA L8 simulator/evaluator.

These are human-facing routing and discussion aliases. They do not replace canonical backend identifiers, repository paths, commit/digest identities, scientific labels, negative classifications, result fields, or failure enums. Normative specifications and machine-readable artifacts must retain canonical identities and may show the codename as an adjacent alias.

This naming directive does not authorize implementation, execution, scoring, seed access, rerun, adoption, merge, or a change in backend status. In particular, assigning `go faster` does not itself reopen the native-CUDA L8 adoption item or alter the current M4 parallel-CPU L8 authority decision.

If any codename is used in a handoff or review, its canonical backend identity and authoritative SHA must remain explicit.
