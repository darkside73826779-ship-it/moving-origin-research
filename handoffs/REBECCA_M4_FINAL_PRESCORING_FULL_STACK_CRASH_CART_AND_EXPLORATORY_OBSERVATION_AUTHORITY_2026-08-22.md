# REBECCA AUTHORITY — M4 FINAL PRE-SCORING FULL-STACK CRASH-CART AND EXPLORATORY OBSERVATION

Date: 2026-08-22 EDT  
Regime: B  
Authority: Rebecca, conveyed and durably routed by WORKFLOW COORDINATOR

## Authorized design requirement

Rebecca requires one final, short, full-stack crash-cart rehearsal after the complete M4 evaluation setup is engineering-clear and before any protected live-scoring release may be considered.

The rehearsal design must use the exact evaluation topology, production harness entrypoints, intended candidate and peer role wiring, authenticated tokenizer/result bindings, runtime/image/dependency identities, GPU execution path, lifecycle, fanout, rollback, evidence, publication, sanitation, and cleanup paths intended for the later scoring run.

This authority requires design and implementation preparation only. It does not authorize the rehearsal to execute.

## Public and synthetic inputs only

The rehearsal must use only deterministic public or synthetic prompts, fixtures, and warmup inputs. It must not access, derive, substitute, consume, expose, or infer protected, holdout, courier, retired, or scientific seed material.

Formal scoring, qualification, threshold decisions, scientific aggregation, constitutional-law satisfaction claims, model ranking, and protected-set inference remain disabled and held.

## Governed warmup

The design must include a deterministic and symmetric prompt-level warmup after exact model/runtime loading and health checks but before measured work.

Candidate and peer must receive the same prospectively fixed public or synthetic warmup sequence with identical generation parameters. Warmup must use no scoring seed stream and must not count toward the measured active window. Warmup outputs and timings must remain visible as separately labeled non-scoring operational evidence.

After warmup, the system must establish a clean synchronized barrier: zero active requests, no surviving conversation or request state, no unauthorized reusable prompt-prefix state, and scoring or measured-run RNG initialized independently after the barrier. Any asymmetric warmup, timeout, error, protected-input substitution, residual state, cache reuse, or RNG carryover must stop fail closed before measured work.

## Minimum measured window

The final rehearsal must contain a minimum thirty-second measured active window. Setup, dependency verification, model loading, health checks, and warmup time do not count toward that minimum.

The exact request count, concurrency, queue capacity, telemetry interval, hard deadline, and resource limits must be fixed prospectively in the reviewed contract. The run may not adapt those controls after observing candidate behavior.

## Complete observation record

Because the rehearsal inputs are public or synthetic, Rebecca requires the complete public observation record to be retained and made available for inspection, including:

- exact prompt and fixture identities;
- candidate and peer outputs;
- token counts and stop reasons;
- request, receipt, lifecycle, and fanout identities;
- ordering, mismatch, error, refusal, truncation, retry, rollback, sanitation, and cleanup evidence;
- per-request timing, latency, throughput, queue, backpressure, and drop evidence; and
- GPU utilization, VRAM, RAM, resource-peak, and cleanup telemetry.

The package must provide both a canonical machine-readable artifact and a concise human-readable export. It must not emit credentials, private custody values or paths, protected inputs, hidden tokenizer arrays, unpublished model bytes, or other prohibited data.

Negative, failed, anomalous, or disagreeing observations must not be discarded merely because they are unfavorable.

## Exploratory trends without scoring

The rehearsal may derive descriptive behavioral and operational trends from its public or synthetic observations, including latency, throughput, output-length, resource-use, agreement/disagreement, stability, error/refusal/stop behavior, and context-length effects.

Every such output must be labeled `NON_SCORING_EXPLORATORY_OBSERVATION`. It may not produce or imply an official score, ranking, qualification, scientific pass or failure, constitutional-law claim, protected-set estimate, or model-readiness conclusion. Structural crash-cart status is an engineering/testbed result only. Replica consistency remains a separate diagnostic axis and cannot be reinterpreted as scientific equivalence.

Crash-cart observations may justify correction of implementation or test-apparatus defects. They may not be used to tune protected prompts, scoring thresholds, scientific bars, seed policy, or evaluation rules to favor either model.

## Fail-closed release boundary

The complete design, implementation, combined-tree identities, model/runtime/gofast identities, commands, schemas, evidence projections, and failure semantics require independent persistent-CRITIC review before Rebecca receives an exact run-release proposal.

Any structural, identity, warmup, lifecycle, evidence, sanitation, cleanup, or governance defect blocks the rehearsal and returns to remediation. No retry, fallback, alternate model/runtime/topology, scoring, science, merge, publication, or gate decision is authorized by this artifact.

## Holds

This authority does not permit:

- model, tokenizer, OCI, or gofast execution;
- private custody or protected-input access;
- scoring, qualification, Q2, EF3, L8 science, or protected-seed work;
- changes to scientific laws, thresholds, controls, or seed rules;
- STATE or provenance mutation;
- merge, result publication, readiness declaration, or final gate decision.

Rebecca retains sole authority for any later exact crash-cart execution release and any separate protected scoring release.

## Expected next event

ARCHITECT may bind this exact immutable authority into the M4 final pre-scoring full-stack crash-cart design correction. The corrected design then returns through WORKFLOW COORDINATOR for one independent persistent-CRITIC rereview. No execution authority is inferred.
