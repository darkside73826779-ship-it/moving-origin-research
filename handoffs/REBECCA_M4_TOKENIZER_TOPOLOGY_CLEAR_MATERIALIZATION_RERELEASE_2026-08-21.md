# Rebecca Re-release — M4 Tokenizer Topology-Clear Materialization

**Date:** 2026-08-21

**Regime:** B

**Status:** EXACT SINGLE OPERATION RE-RELEASED

Rebecca authorizes the still-unconsumed bounded tokenizer-materialization operation to proceed after closure of CR-MNT1, CR-MNT2, and CR-MNT3.

Exact public inputs:

- Cleared topology package: `architect/m4-tokenizer-cr-mnt3-identity-reconciliation` at `522b01d42ceef90ee2390c169f29fb163e610470`
- Topology review result: `critic/m4-tokenizer-cr-mnt3-final-identity-rereview` at `41d844063416b68090c933a76f31e82c4d83e458`
- Topology verdict: `COMBINED CLEAR`
- Banked CR-MNT1/CR-MNT2 review: `critic/m4-tokenizer-topology-sequence-negative-rereview` at `5a4b19e0d08ff5c87d0c6439b47007e3a55ac759`
- Official WSL2 pre-execution test bed: `coordinator/m4-wsl2-preexecution-testbed` at `0631a019a153ec89312938982c007cf94dd3f03e`, tag `m4-wsl2-preexecution-testbed-v1.1`
- Preserved private-binding release: `coordinator/m4-tokenizer-private-binding-rerelease` at `a4d780b11c9719df77acd320212a296542e5871a`
- Prior no-consumption instrument failure: `taskbuilder/m4-tokenizer-private-binding-materialization-execution` at `1f0f41b42e28b0edb8397db3681131755163f7b7`

The previously supplied host-private custody binding remains the only authorized binding. It is re-supplied to TASK BUILDER through private process-local coordination and is intentionally absent from this public artifact. No alternate root, directory search, fallback, synthesis, normalization, copying, or invention is authorized.

TASK BUILDER must use a fresh immutable checkout of this exact release and execute the committed order exactly:

1. verify the released checkout and governed raw identities;
2. run the exact pinned no-custody wrapper;
3. stop on any nonzero result;
4. run the exact custody-free topology smoke matrix, including required positive and negative checks;
5. stop on any nonzero, nonempty-stage, topology, cleanup, or identity failure;
6. verify the existing private binding without publishing it;
7. only after every pre-operation gate passes, perform exactly one materializer invocation.

The earlier engine exit 125 occurred before container start and is governed as no consumption. The single materializer operation therefore remains unconsumed at release. No materializer retry is authorized after the materializer process starts. No fallback, alternate image, interpreter, runtime, mount topology, command, or custody binding is authorized.

Only the contract-permitted sanitized public result and handoff may be committed and pushed. Local paths, custody records, host environment values, model/tokenizer bytes, complete token arrays, rendered private text, credentials, and machine identifiers remain prohibited from Git and public artifacts.

All inference/serving, Q2/EF3, qualification, diagnostics/scoring, protected seeds, scientific execution, STATE/provenance mutation, model/tokenizer publication, merge, rerun, and further-operation holds remain binding.

Public-safety classification: the fixed-regex preflight findings on the official test-bed head and prior instrument-failure head are digit substrings wholly inside the required immutable public Git commit identities shown above. They are reproducibility metadata, not personal contact data. The duplicate commit-parent/range-domain findings are retained and manually classified; none is suppressed. Gitleaks reports zero findings for this release.
