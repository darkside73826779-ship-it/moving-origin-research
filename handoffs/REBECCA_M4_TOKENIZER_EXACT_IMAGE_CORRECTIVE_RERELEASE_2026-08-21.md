# Rebecca M4 Tokenizer Exact-Image Corrective Re-release

Date: 2026-08-21  
Regime: B  
Authority: Rebecca R. McClintic  
Disposition: APPROVED — ONE EXACT INVOCATION

## Preserved authority

- Prior command re-release: `coordinator/m4-tokenizer-oci-command-rerelease` at `2811c35bf62bd1c597a4acfef2f10c57fd36d821`.
- Amended contract: `architect/m4-tokenizer-materialization-spec` at `d94c059645dba018bbf2d9adf1b31c388dfcd2a3`.
- Persistent-CRITIC CLEAR: `critic/m4-tokenizer-oci-lf1-p4-rereview` at `cf372859aa2306b9e630331ed66339954328ac37`.

## Disposition of the pre-operation error

The denied registry-resolution attempt under an incorrect repository name did not start a container process, invoke the prescribed materializer, perform custody lookup, access model/tokenizer bytes, create output, or consume the bounded materialization operation. It is preserved as a pre-operation operator/reference error and does not authorize a fallback.

## Exact corrective authority

Rebecca authorizes exactly one invocation using the already-present pinned image under the contract repository identity `docker.io/vllm/vllm-openai`, with the exact approved index/platform identities and the committed `python3 -I` command, classifier, resolver, custody, schema, fixture, canonicalization, and test contract.

This authority permits no registry-name variation, alternate interpreter/runtime/image, image modification, symlink, search, download, fallback, or further retry. Before invoking the materializer, TASK BUILDER must verify the local image reference resolves to the exact approved platform digest. Any mismatch is fail-closed and returns to WORKFLOW COORDINATOR without custody access.

## Preserved holds

No inference/serving, Q2/EF3, qualification, diagnostics/scoring, protected seeds, science/bar/control/battery change, STATE/provenance mutation, model/tokenizer publication, merge, or additional materialization operation is authorized. Local custody values, paths, environment/machine identity, and model/tokenizer bytes remain outside Git.

## Next event

Replacement persistent TASK BUILDER executes the single exact invocation if every precondition passes, then returns sanitized contract-permitted evidence directly to WORKFLOW COORDINATOR. No additional correction or retry is inferred.
