# Rebecca M4 Tokenizer OCI Command Re-release

Date: 2026-08-21  
Regime: B  
Authority: Rebecca R. McClintic  
Disposition: APPROVED FOR ONE BOUNDED OPERATION

## Exact approved lineage

- Preserved bounded-materialization release: `coordinator/m4-tokenizer-materialization-release` at `9007260570235c1b06b104d78106ba32e8a4e9dd`.
- OCI-environment release: `coordinator/m4-tokenizer-oci-environment-release` at `2e7fb9ee1f9643405d7fbc40120c7dc383abf9a3`.
- Amended contract: `architect/m4-tokenizer-materialization-spec` at `d94c059645dba018bbf2d9adf1b31c388dfcd2a3`.
- Amendment review: `critic/m4-tokenizer-oci-lf1-p4-rereview` at `cf372859aa2306b9e630331ed66339954328ac37`.
- Review artifact: `reviews/critic_m4_tokenizer_oci_lf1_p4_rereview.md`.

## Rebecca decision

Rebecca explicitly re-releases the still-unconsumed single bounded tokenizer-materialization operation against the exact amended contract and CRITIC CLEAR above.

The executor may use the already authorized exact pinned WSL2-backed OCI environment and the amended `python3 -I` command. The pure no-process-spawn launcher-disposition classifier and its date/regime-bound fixtures are part of the approved contract. This is a re-release of the original one-operation authority, not a second operation or permission to retry after the operation begins.

## Authorized executor and stop conditions

The replacement persistent TASK BUILDER may perform exactly the released operation. It must follow the committed resolver, custody, runtime, command, schema, fixture, canonicalization, test, and publication boundaries without substitution or invention.

Any identity, digest, custody, runtime, schema, command, output, or test mismatch is fail-closed and returns through the WORKFLOW COORDINATOR. No fallback, alternate image, alternate interpreter, host-runtime substitute, image modification, symlink, search, or rerun is authorized.

## Preserved holds

This ruling does not authorize inference or serving; Q2 or EF3 changes; qualification; diagnostics or scoring; protected-seed access; scientific, bar, control, or battery changes; STATE or provenance mutation; model/tokenizer publication; merge; or any additional acquisition/materialization operation.

The operation remains local-only. Model, tokenizer, checkpoint, cache, adapter, conversion, local custody, private path, environment, and machine-identity material must not enter Git. Only sanitized contract-permitted evidence may be committed.

## Next event

WORKFLOW COORDINATOR routes this exact authority to the replacement persistent TASK BUILDER. TASK BUILDER either returns the exact bounded result or a fail-closed BLOCK; it does not infer additional authority.
