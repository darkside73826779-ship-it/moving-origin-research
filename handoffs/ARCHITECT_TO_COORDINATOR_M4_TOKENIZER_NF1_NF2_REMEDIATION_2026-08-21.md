# FORMAL HANDOFF — PERSISTENT ARCHITECT → WORKFLOW COORDINATOR — TOKENIZER NF1/NF2 REMEDIATION

**Timestamp:** 2026-08-21 EDT
**Work item/gate:** M4 tokenizer NF1/NF2 pre-import runtime validation
**Sender:** replacement persistent ARCHITECT
**Intended receiver / next owner:** WORKFLOW COORDINATOR for routing to persistent CRITIC
**Authority:** `coordinator/m4-tokenizer-if2-execution-rerelease@902df984a2e689d41954d749fcf06570b42d9c4b`
**Input:** `architect/m4-tokenizer-materialization-spec@903a0c3a032b672a6b9a3a32bb53f66b4f1f07f5`
**Authoritative finding:** `critic/m4-tokenizer-normative-json-lf-rereview@41fc77360b431ef9fbeb3e84772124324cb01601`

## Return

NF1 is remediated prospectively by the exact isolated command `python3 -I tests/run_m4_tokenizer_materialization_tests.py`. The prescribed future wrapper validates the banked ordered 9+9 raw mounted-worktree paths before project/test import, custody lookup, or output, then and only then invokes the preserved standard-library discovery inputs. Callable, root resolution, snapshot fields, seven-step short-circuit order, canonical failure destination, and exit mapping are fixed without runtime normalization, copying, installation, path injection, or alternate runtime `[PROPOSED]`.

NF2 is remediated by the two committed negative artifacts `specs/data/m4_tokenizer_runtime_validation_negative_cases_v1.json` and `specs/data/m4_tokenizer_runtime_validation_negative_expected_v1.json` plus sidecars. Ten fixed test IDs and exact single-mutation operations cover missing/extra path, attribute, link, CR, BOM, terminal LF, sidecar grammar/basename, and digest mismatch in reachable validation order. Mutations operate only on fresh in-memory snapshots after the production positive validation and never modify the mounted checkout `[PROPOSED]`.

Affected normative artifacts are `.gitattributes`, `specs/m4_local_tokenizer_materialization_spec_v1.md`, `specs/m4_local_tokenizer_materialization_changelog.md`, the materialization test contract and sidecar, and the two negative JSON/sidecar pairs. The positive nine JSON plus nine sidecar identities, IF1/IF2, pinned image/runtime, unchanged materializer command, custody/schema/fixture identities, and scientific construction are preserved.

## Holds and next event

The single materialization operation remains unconsumed. Custody, model, and tokenizer bytes were not read; OCI/materialization was neither executed nor modified. Inference/serving, Q2/EF3, qualification, diagnostics/scoring, seeds, science, STATE/provenance, publication, rerun, merge, and gate decision remain held. Next expected event is Coordinator receipt and persistent-CRITIC NF1/NF2 rereview. Ownership transfers only on Coordinator acknowledgement.
