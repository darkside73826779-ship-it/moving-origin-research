# REBECCA AUTHORITY — M4 FINAL PRE-SCORING CRASH-CART BETA PROMOTION AND IMPLEMENTATION

Date: 2026-08-22 EDT

Regime: B

Authority: Rebecca, conveyed and durably routed by WORKFLOW COORDINATOR

## Decision

Rebecca authorizes promotion of the independently cleared M4 final pre-scoring crash-cart design from `v0.2-alpha` to `beta` and authorizes exact implementation of that beta design.

This is an implementation-only decision. It does not authorize execution of the crash cart or any scoring activity.

## Immutable basis

- Corrected design route: `architect/m4-final-prescoring-crash-cart-bf1-bf4-correction` at `757dc3bf3b7eec554ee15882af94fce4a6ebb006`.
- Corrected substantive result: `7674fb1a9b2762fd7fafb68f9547f22da83194b5`.
- Canonical manifest: `architect/m4-final-prescoring-crash-cart-bf1-bf4-correction-manifest-v2` at `fd639f4a8c116e75c3728ed10411e171c0de4367`.
- Authoritative persistent-CRITIC `COMBINED CLEAR`: `critic/m4-final-prescoring-crash-cart-bf1-bf4-rereview` at `76b6b99444f0ecf0f085681c6eddb454f8cf2a60`.
- Rebecca crash-cart observation authority: `coordinator/m4-final-crash-cart-rebecca-authority` at `8b4a383c450e8b29f2633f7e25107abcb62dd929`.
- Rebecca artifact-versioning authority: `coordinator/m4-final-crash-cart-versioning-authority` at `086d6a4ab34044bb8b9d637b5e575e47f8863ee2`.

The blocked `v0.1-alpha` and corrected `v0.2-alpha` identities remain immutable provenance. Promotion does not relabel either historical artifact.

## Authorized implementation scope

TASK BUILDER may implement only the exact cleared beta design, including its:

- four-pair public warmup and clean synchronized barrier;
- independent post-warmup RNG insertion point;
- 64 paired measured requests over the governed active window and deadline;
- queue, telemetry, lifecycle, fanout, rollback, sanitation, cleanup, and deterministic-export contracts;
- five-stage fail-closed report representation;
- separate structural and replica-consistency axes;
- complete non-scoring exploratory-trend evidence; and
- exact HELD-only law projection and no-scoring/no-qualification boundaries.

Every presently `UNBOUND` implementation, combined-tree, runtime, model, gofast, semantic-validator, wrapper, or release identity remains a fail-closed no-start condition until implemented, reviewed, and separately released.

## Required implementation return

TASK BUILDER shall return one canonical, remotely equal implementation package with:

1. exact source, tests, schemas, launch contract, wrapper, inventory, and sidecars;
2. custody-free synthetic and public-fixture evidence for all success and staged failure paths;
3. warmup exclusion and zero-based post-reset ordinal evidence;
4. structural/replica-axis and exact-replica consumer-stop regressions;
5. deterministic cleanup, rollback, sanitation, and no-fabricated-evidence regressions;
6. complete identity and public-safety validation; and
7. an explicit statement that no model run, protected input, scoring, or science occurred.

The exact package then requires one authoritative persistent-CRITIC implementation review before any executable release can be considered.

## Holds

This authority does not authorize:

- model, tokenizer, OCI, WSL2 workload, or gofast execution;
- a crash-cart feasibility run;
- private custody, protected prompts, protected seeds, or held inputs;
- scoring, qualification, law adjudication, scientific interpretation, or trend publication;
- promotion to `v1`;
- merge of the implementation package to `main`; or
- any readiness, execution-release, scoring-release, or final gate decision.

Only a later explicit Rebecca decision may authorize a reviewed executable beta release or run.

## Workflow economy

The implementation lane shall reuse banked reviewed evidence, avoid duplicate audits and tests, use subagents only for bounded independent work, retire them immediately after integration, and produce one batched return rather than serial micro-remediation handoffs.
